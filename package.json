{
  "name": "orderix-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build:prod": "cd server && npm install && npx prisma generate && cd ../client && npm install && npm run build",
    "install:all": "npm install --workspaces",
    "postinstall": "cd server && npx prisma generate"
  }
}
